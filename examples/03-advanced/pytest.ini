[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -v --tb=short -m "not e2e"

markers =
    unit: Fast unit tests — no I/O, all dependencies mocked
    integration: Integration tests — SQLite DB, no Docker required
    e2e: End-to-end tests — requires running Docker Compose stack
